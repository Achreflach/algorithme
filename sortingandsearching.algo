PROCEDURE Insertionsort (A: 3, 5, 11, 19, 55)
   n = length(A)
   FOR i = 1 to n - 1 DO
       j = i
       while j > 0 and A[j-1] > A[j] do
           swap(A[j], A[j-1])
           j = j - 1
       END while
   END FOR
END PROCEDURE